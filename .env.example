# dotfile-automation configuration template
#
# Copy this file to ~/.local/share/dotfile-automation/.env and customize
# for your system. Each script sources this file to get configuration.
#
# All variables have defaults if not set, but you can customize them here.

# ============================================================================
# CORE PATHS - Required for all scripts
# ============================================================================

# Where your dotfiles repository lives (where source files are stored)
# Default: $HOME/.dotfiles
DOTFILES_DIR="$HOME/.dotfiles"

# Path to your symlink registry file (dotfiles.conf)
# Default: $DOTFILES_DIR/dotfiles.conf
CONF_FILE="$DOTFILES_DIR/dotfiles.conf"

# Which shell configuration file gets env var exports
# Use ~/.zshenv for zsh, ~/.bashrc or ~/.bash_profile for bash
# Default: $HOME/.zshenv
SHELL_ENV_FILE="$HOME/.zshenv"

# ============================================================================
# XDG BASE DIRECTORY SPECIFICATION
# ============================================================================
# See: https://specifications.freedesktop.org/basedir-spec/latest/
#
# These determine where tools store config, cache, and data files when
# they're migrated from HOME by home-cleanup.sh

# XDG config directory (for ~/.config/* files and env vars)
# Default: $HOME/.config
export XDG_CONFIG_HOME="$HOME/.config"

# XDG cache directory (for cache files like npm, pip, etc)
# Default: $HOME/.cache
export XDG_CACHE_HOME="$HOME/.cache"

# XDG data directory (for application data and state)
# Default: $HOME/.local/share
export XDG_DATA_HOME="$HOME/.local/share"

# XDG state directory (for runtime state, logs, etc)
# Default: $HOME/.local/state
export XDG_STATE_HOME="$HOME/.local/state"

# ============================================================================
# FEATURE FLAGS - home-cleanup.sh
# ============================================================================
# Set to "false" to skip migration of specific tools you don't use.
# By default, all tools are migrated (if they exist on your system).

# Package managers and foundations
INCLUDE_NPM="true"
INCLUDE_PYTHON="true"
INCLUDE_DOTNET="true"
INCLUDE_NUGET="true"

# Language runtimes
INCLUDE_NVM="true"
INCLUDE_BUN="true"
INCLUDE_GEM="true"
INCLUDE_PUB_CACHE="true"

# Development tools
INCLUDE_DOCKER="true"
INCLUDE_OH_MY_ZSH="true"
INCLUDE_ANDROID="true"

# Terminal environments
INCLUDE_ITERM2="true"
INCLUDE_DART="true"
INCLUDE_VIM="true"

# ============================================================================
# ADVANCED - Rarely changed
# ============================================================================

# Enable/disable color output (auto-detected from terminal, can override)
# Set to "false" to disable colors (useful for piping to files/scripts)
# USE_COLORS="true"

# Debug mode (verbose output from scripts)
# Set to "true" for debugging
# DEBUG="false"

# ============================================================================
# NOTES
# ============================================================================
#
# 1. This file is sourced by all scripts using:
#    source "$DOTFILE_AUTOMATION_DIR/lib.sh"
#    (lib.sh calls load_env internally)
#
# 2. All paths can use ~ for home directory (scripts expand it)
#
# 3. Default values are applied in lib.sh if variables are not set here
#
# 4. The .env file is in .gitignore so your personal paths stay private
#
# 5. For more details on configuration, see:
#    - README.md (overview)
#    - USAGE.md (command reference)
#    - XDG-SETUP.md (XDG Base Directory explanation)
