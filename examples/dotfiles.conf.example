# dotfiles.conf - Registry of managed dotfiles
#
# Format: source:description:symlink_destination
#
#   source              = Path to the canonical file in your dotfiles repo
#   description         = Human-readable description (shown in status output)
#   symlink_destination = Where the symlink lives (what the system reads)
#
# Rules:
#   - Lines starting with # are comments
#   - Empty lines are ignored
#   - Use ~ for home directory (scripts will expand it)
#   - Fields are separated by colons (:)
#
# To use this file:
#   1. Copy it to your dotfiles directory:
#      cp examples/dotfiles.conf.example ~/.dotfiles/dotfiles.conf
#   2. Edit it to match your actual dotfiles
#   3. Run: symlink-check.sh --dry-run

# ---------------------------------------------------------------------------
# Shell configuration
# ---------------------------------------------------------------------------
~/.dotfiles/.zshrc:Zsh main configuration:~/.zshrc
~/.dotfiles/.zshenv:Zsh environment variables:~/.zshenv
~/.dotfiles/.bashrc:Bash configuration:~/.bashrc
# ~/.dotfiles/.bash_profile:Bash profile:~/.bash_profile

# ---------------------------------------------------------------------------
# Git
# ---------------------------------------------------------------------------
~/.dotfiles/.gitconfig:Git configuration:~/.gitconfig
~/.dotfiles/.gitignore_global:Global gitignore rules:~/.gitignore_global

# ---------------------------------------------------------------------------
# Terminal emulators (uncomment the one you use)
# ---------------------------------------------------------------------------
# ~/.dotfiles/config/alacritty/alacritty.toml:Alacritty config:~/.config/alacritty/alacritty.toml
# ~/.dotfiles/config/kitty/kitty.conf:Kitty config:~/.config/kitty/kitty.conf
# ~/.dotfiles/config/wezterm/wezterm.lua:WezTerm config:~/.config/wezterm/wezterm.lua
# ~/.dotfiles/ghostty/config:Ghostty config:~/.config/ghostty/config
# ~/.dotfiles/.iterm2_shell_integration.zsh:iTerm2 shell integration:~/.iterm2_shell_integration.zsh

# ---------------------------------------------------------------------------
# Editors (uncomment what you use)
# ---------------------------------------------------------------------------
# ~/.dotfiles/config/nvim/init.lua:Neovim configuration:~/.config/nvim/init.lua
# ~/.dotfiles/.vimrc:Vim configuration:~/.vimrc

# ---------------------------------------------------------------------------
# Prompt / shell tools
# ---------------------------------------------------------------------------
# ~/.dotfiles/config/starship.toml:Starship prompt config:~/.config/starship.toml

# ---------------------------------------------------------------------------
# Multiplexers
# ---------------------------------------------------------------------------
# ~/.dotfiles/.tmux.conf:Tmux configuration:~/.tmux.conf
# ~/.dotfiles/config/zellij/config.kdl:Zellij config:~/.config/zellij/config.kdl

# ---------------------------------------------------------------------------
# Other tools
# ---------------------------------------------------------------------------
# ~/.dotfiles/.mackup.cfg:Mackup sync configuration:~/.mackup.cfg
# ~/.dotfiles/.todo.cfg:Todo.txt configuration:~/.todo.cfg
# ~/.dotfiles/config/htop/htoprc:htop configuration:~/.config/htop/htoprc
# ~/.dotfiles/config/bat/config:bat configuration:~/.config/bat/config
